# decorator_simple_test.jg — 验证新 @decorator 语法

@get("/api/hello")
[hello()]: {
  response(status=200, body={"message": "hello from decorated function"})
}

@post("/api/echo")
[echo()]: {
  response(status=200, body=input.body)
}

[start]: serve()
